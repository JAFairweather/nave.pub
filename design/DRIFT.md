# Design-system drift

Generated by `bin/nave-drift` on 2026-08-05. **Do not hand-edit.**

Verdicts: `ok` byte-identical · `stale` re-vendor · **`diverged`** a shared
component disagrees with both the hub and the baseline (AD-11: promote, never fork) ·
`not adopted` carries its own implementation · `missing` no such file.

| artifact | repo | verdict | detail |
|---|---|---|---|
| `design/tokens.css` | nvoy | stale | absent: --ground --panel-2 --line-strong --field +11 |
| `design/tokens.css` | nact | **diverged** | redefined: --mono |
| `design/tokens.css` | ngage | **diverged** | redefined: --panel --dim --faint --mono +1 |
| `design/tokens.css` | waggle | not adopted | private token block, not a vendored copy — adoption tracked in nave.pub#113 |
| `components/nave-titlebar.mjs` | nvoy | stale | matches the last published hash; hub has moved ahead |
| `components/nave-titlebar.mjs` | nact | stale | matches the last published hash; hub has moved ahead |
| `components/nave-titlebar.mjs` | ngage | stale | matches the last published hash; hub has moved ahead |
| `components/nave-titlebar.mjs` | waggle | stale | matches the last published hash; hub has moved ahead |

0 ok · 5 stale · 2 diverged · 1 not adopted · 0 missing
